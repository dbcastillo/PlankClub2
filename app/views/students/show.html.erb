<h2>Student Info:</h2>

<% if @student.img_url.blank? %>
  <%= image_tag "Human.png" %>
<% else %>
  <%= image_tag(@student.img_url) %>
<% end %>

<hr>
<hr>
Name: <%= @student.name %>
<br>
Class Number: <%= @student.class_number  %><br>
Planks Accomplished: <br>
<% if @student.id == current_student.id %>
<ul>
  <% planktypes = @student.student_planks.each do |studentPlank| %>
    <li><%= studentPlank.info %>
      <%= link_to "Edit Plank", edited_plank_path(@student, studentPlank) %>
    -- <%= link_to "Delete Plank",
    deleted_plank_path(@student, studentPlank), method: :delete, data: { confirm: 'Are you sure?' }  %></li>
  <% end %>
</ul>
<h3><%= link_to "Log New Plank", new_student_plank_path(@student) %> </h3>
<h3><%= link_to "Edit Student", edit_student_path(@student) %></h3>
<h3><%= link_to "Delete Student",
student_path(@student), method: :delete, data: { confirm: 'Are you sure?' } %></h3>
  <% else %>
  <% planktypes = @student.student_planks.each do |studentPlank| %>
    <li><%= studentPlank.info %>
  <% end %>
<% end %>
