<% @planks.each do |plank| %>
<%= plank.planktype %> :
<% plank.students.each do |student| %>
<ol> <%= student.name %> : <%= (student.student_planks.select {|student_plank| student_plank.plank == plank})[0].duration %> </ol>
<% end %>
<br></br>
<% end %>
